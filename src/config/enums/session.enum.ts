export enum ENUM_TEST_TYPE {
  DIAGNOSTIC = 'diagnostic',
  CUSTOM = 'custom',
  CHAPTER_TEST = 'chapter_test',
  QUIZZ = 'quizz'
}

export enum ENUM_TEST_STATUS {
  NOT_STARTED = 'not_started',
  IN_PROGRESS = 'in_progress',
  PAUSED = 'paused',
  COMPLETED = 'completed',
  ABANDONED = 'abandoned'
}

export enum ENUM_SESSION_REFERENCE_TYPE {
  TEMPLATE_MODULE = 'template_module',
  TEMPLATE = 'template'
}